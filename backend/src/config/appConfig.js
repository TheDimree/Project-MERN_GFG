// import dotenv from "dotenv";
// // dotenv.config(); // * Automatically loads .env from the root directory
// dotenv.configDotenv({path: "../../.env"})

// // Configuration settings
// const appConfig = {
//   PORT: process.env.PORT ?? 8000, // App's port (default to 5000)
//   DB_URL: process.env.CON_URL ?? "mongodb://localhost:27017/merndemoproject",  // MongoDB UR, merndemoproject is the db name
//   JWT_SECRET: process.env.JWT_SECRET?? "your_super_secret_key_2414677793!@#$%^&*()",  // JWT secret key (default to "mysecretkey")
// };

// // console.log("appConfig: ", appConfig)
// export default appConfig;